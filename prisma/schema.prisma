generator client {
  provider = "prisma-client-js"
  //previewFeatures = ["mongodb"]
}

datasource db {
  provider = "mongodb"
  url      = env("mongodb+srv://sebastian:12345678rl@cluster0.cpyt3yx.mongodb.net/Contador")
}

model User {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  contador Int

  
 
}